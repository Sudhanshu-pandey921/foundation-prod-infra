trigger:
  - none

pool: prod-pool

stages:
  - stage: FirstStage
    displayName: TerraformInstall

    jobs:
      - job: InstallTerraform
        steps:
          - task: TerraformInstaller@1
            inputs:
              terraformVersion: 'latest'
    
          
            